packages:
  - "apps/*"

# 의존성 설치 시 빌드 스크립트 실행 허용 목록 (pnpm 11 보안 기본값 대응)
allowBuilds:
  "@prisma/client": true
  "@prisma/engines": true
  esbuild: true
  prisma: true
  sharp: true

# Node engines 하한을 강제한다 (Node 24 미만 설치 차단)
engineStrict: true
