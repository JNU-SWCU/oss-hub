import type { NavItem } from '@/components';

/**
 * 공개 화면 메뉴 — 로그인 없이 볼 수 있는 화면의 **라벨·경로 단일 원본**이다.
 * 랜딩 헤더(`layout.tsx` → `ShellNav`)와 업무 사이드바(`sidebar-menu.ts`)가
 * 이 목록 하나를 함께 읽는다. `role-menus.ts`가 역할 메뉴에 대해 하는 일과 같다 —
 * 사이드바는 여기에 아이콘과 묶음만 얹고, 문구를 다시 적지 않는다.
 *
 * 두 셸에 손으로 따로 적어 두었다가 실제로 갈라졌다(#513). 같은 화면을 헤더는
 * `아카이브`, 사이드바는 `공개 아카이브`로 불렀고, 사이드바에는 `/ranking`이
 * 아예 없어서 **로그인한 사용자에게는 랭킹으로 가는 길이 없었다**.
 *
 * `/`는 여기 없다 — 두 셸 모두 브랜드(`OSS Hub`)가 이미 `/` 링크이기 때문이다.
 * 자세한 근거는 아래 주석 참고.
 *
 * 역할 메뉴(`role-menus.ts`)와 계정 묶음은 여기 올라오지 않는다. 가입을 마치지
 * 않은 방문자에게 눌러도 튕기는 메뉴를 미리 보여 주지 않는다는 구분이며,
 * 의도적으로 유지한다(#512). 공유되는 것은 **공개 화면뿐**이다.
 */
export const PUBLIC_MENU: NavItem[] = [
  { label: '프로그램', href: '/programs' },
  /**
   * `아카이브`가 아니라 `공개 아카이브`다. 이 제품이 이 화면을 부르는 이름이
   * 원래 그쪽이다 — 화면 제목(`archive-list-view.tsx`), 랜딩 여정의 STEP 4,
   * API 오류 문구가 모두 `공개 아카이브`를 쓴다. 짧은 쪽을 쓰던 곳은 손으로 적은
   * 랜딩 헤더 하나뿐이었다.
   *
   * `공개`는 장식이 아니라 정보다. 여기 실리는 저장소는 공개에 동의한 것만이고,
   * 자기 작업이 어디까지 드러나는지가 학생에게는 실제 질문이다.
   */
  { label: '공개 아카이브', href: '/archive' },
  { label: '랭킹', href: '/ranking' },
];
