# Codex 작업 저널

## 2026-09-09 — 신청 반려 결과와 복구 경로 안내

- 상태: review
- Issue: 신청 반려 결과 안내의 새 이슈를 PR 본문에 연결한다.
- PR: 이 PR.
- blocker: 없음.
- 내용: 신청 반려 확인창에서 학생이 스스로 다시 신청할 수 없다는 결과와 교직원의 「검토 대기로」 복구 경로를 확정 전에 알린다.
- 접근성: 결과 안내를 확인창 설명에 연결하고 사유 입력칸의 이름·오류·학생 노출 안내를 유지한다.
- 범위: 승인·되돌리기 확인창, 서버 판정과 권한, 반려 학생의 서류 버튼 잠금은 변경하지 않는다.
- 검증: frontend lint 0오류·기존 경고 5개, typecheck 통과, 전체 352파일·3,551테스트 통과.
  반려 확인창 32테스트, 변경 파일 포맷과 diff 공백 검사도 통과했다.
- 검증 환경: 기본 worker 병행 실행에서 기존 시험 3건이 5초 제한을 넘었다.
  코드를 바꾸거나 시험을 제외하지 않고 worker 2개로 전체를 순차 재실행해 통과했다.
- 화면 증거와 확인 경로는 PR 본문에서 관리한다.
- 공개 안전: 합성 데이터만 사용하며 비밀값·실데이터·개인정보·내부 호스트·로컬 경로를 포함하지 않는다.

## 2026-09-09 — 제출 화면에 실제 서버 업로드 상한 전달

- 상태: review
- Issue: #1210 — 업로드 상한의 화면 사본 제거
- PR: (이 PR)
- blocker: 없음
- 변경: 최초 제출 폼과 내 제출 체크리스트 응답에 서버의 파일 상한을 추가했다.
  프로그램 작성 권한을 확인하는 업로드 정책 조회 API를 추가했다.
  화면이 별도 기본값을 유지하지 않고 서버 값을 받아 사용할 수 있도록 하는 선행 API 변경이다.
- 범위: 파일 형식 정책과 업로드 검증·저장 동작을 유지했다.
  응답 계약을 먼저 배포한 뒤 이를 소비하는 클라이언트를 배포한다.
- 검증: main 8510328d 기반의 동일한 backend 소스에서 단위 318개 스위트·3,651개 테스트와 격리 통합 97개 스위트·567개 테스트 통과, typecheck 통과, 변경 파일 포맷 검사 통과.
  서버 상한을 2 MB로 바꾼 합성 입력으로 세 응답이 같은 상한을 반환하고, 프로그램 작성 권한이 없는 요청을 거절하는 HTTP 검증을 포함한다.

## 2026-09-09 — 업로드 상한을 서버 응답으로 통일

- 상태: review
- Issue: #1210 — 업로드 상한의 화면 사본 제거
- PR: (이 PR)
- blocker: 업로드 정책 응답 API를 먼저 배포해야 한다.
- 변경: 최초 제출·다시 제출·교직원 프로그램 작성의 상한 표시와 검증이 서버 응답을 사용한다.
  제한을 넘는 파일을 고르면 제출 버튼을 누르기 전에 오류를 표시하고, 유효한 파일로 바꾸거나 선택을 취소하면 오류를 지운다.
  기존 교직원 양식 입력의 오류와 입력 컨트롤을 접근성 속성으로 연결했다.
- 범위: 최신 main의 학생 PDF/HWP/ZIP 정책과 교직원 양식 형식 목록을 유지했다.
  반려 학생 버튼 잠금과 다시 게시 문구는 변경하지 않았다.
- 검증: main 8510328d 기반의 동일한 frontend 소스에서 352개 파일·3,559개 테스트 통과, typecheck 통과, lint 오류 0개·기존 경고 5개, 변경 파일 포맷 검사 통과.
- 인터뷰 반영: 화면 종류와 관계없이 파일 선택 시점에 초과 여부를 알리고 서버 한도와 같은 안내를 사용하도록 한 요청을 반영했다.
