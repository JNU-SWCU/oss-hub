interface:
  display_name: 'OSS Hub QA 티켓 관리'
  short_description: '영역별 증거와 selector 캡처를 갖춘 Notion QA 티켓을 작성·검증합니다'
  default_prompt: 'Use $manage-qa-tickets to create, deduplicate, rewrite, verify, migrate, or publish an executable OSS Hub QA ticket, declaring its frontend, backend, or infra area and capturing any frontend screen by DOM selector.'
