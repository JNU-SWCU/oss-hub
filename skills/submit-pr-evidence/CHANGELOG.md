# Changelog

- 2026-09-08 — v1.7.1: v1.7.0의 「답글에 쓰지 않는 것」 목록이 **위젯이 말하지 않는 것까지 지우고 있었다** — (1) `최종 통합 E2E 66개…가 통과했습니다`를 「checks 줄이 말한다」며 금지했는데 browser E2E는 CI lane이 아니라 로컬 수동 게이트이고([`.github/workflows/ci.yml:321`](https://github.com/JNU-SWCU/oss-hub/blob/main/.github/workflows/ci.yml#L321), AGENTS.md), 어떤 위젯도 그것을 표시하지 않으므로 오히려 같은 절이 「답글에 반드시 쓰라」고 한 부류였다. (2) `3,533개 테스트가 통과했습니다`의 금지 근거를 「checks 줄이 말한다」로 댔는데 checks 줄이 말하는 것은 lane이 초록이라는 것뿐이고 개수는 말하지 않는다 — 절차 7단계가 개수를 요구하는 이유가 정확히 그것이라 근거가 스킬 자신의 논지와 반대였다. (3) 수치의 자리를 PR 본문 `## 검증` 절 하나로 고정했지만 그 절은 PR을 열 때 쓰고 끝나는 절이라, **리뷰 대응 커밋의 재검증 수치는 답글에도 본문에도 갈 자리가 없었다** — 이 규칙이 가장 자주 적용될 경로에 구멍이 나 있었다 → E2E를 「초록불이 감추는 것」 쪽으로 옮기고, 개수 예시의 근거를 「본문 `## 검증` 절이 말한다」로 고치고, 「`## 검증`은 PR을 열 때 쓰고 끝나는 절이 아니다 — 다시 돌렸으면 갱신한다」를 명시했다. 덤으로 커밋 해시 금지가 「어느 지적을 어느 커밋에서 고쳤는가」까지 지우지 않도록, 「병합하지 않았습니다」 금지가 「보류했다」·「승인을 기다린다」로 일반화되지 않도록 각 불릿에 경계를 달았다. v1.7.0 항목의 `3,5NN개` 마스킹도 뗐다 — 가릴 이유가 없는 값이고 `SKILL.md`는 그대로 적고 있었다. Provenance: 2026-09-08 code-reviewer lane이 #1238 병합 뒤 보고한 must-fix 3건을 `ci.yml`·`AGENTS.md`에서 직접 재확인한 뒤 반영.
- 2026-09-08 — v1.7.0: 스킬이 PR 본문 작성은 여섯 원칙으로 규율하면서 **리뷰 답글에는 아무 규칙도 두지 않았고**, 절차 7단계의 「검증 보고에는 실행된 suite·test 개수를 적는다」가 어느 표면을 말하는지도 적혀 있지 않았다 — 그 결과 PR #1230·#1231·#1232의 답글이 세 곳 모두 거의 같은 문단으로 끝났다: 「전체 프런트엔드 3,533개 테스트와 빌드·타입·형식 검사를 통과했고」, 「최종 통합 E2E 66개와 이 커밋의 CI·public-safe·commitlint가 통과했습니다」, 「후속 커밋은 …입니다」, 「병합·배포는 하지 않았습니다」. 넷 다 PR 화면의 checks 줄·커밋 목록·PR 상태가 이미 말하고 있는 것이라, 리뷰어는 자기가 방금 본 것을 다시 읽으며 정작 받아야 할 답을 그 사이에서 찾아야 했다 — AP-19(위젯이 이미 말한 것)를 화면이 아니라 PR 표면에서 되풀이한 셈이다 → `## 리뷰 답글 작성 원칙`을 새로 두어 **초록불이 말해 주는 것은 쓰지 않고 초록불이 감추는 것을 쓴다**를 규칙으로 박고, 답글에 남겨야 하는 셋(지적 한 건에 대한 답 / 재실행으로 통과한 검사·원인 미확정·못 돌린 검증·보류 항목 / 리뷰어가 다음에 할 행동)을 명시했다. 수치는 없애지 않고 자리를 하나로 고정했다 — 절차 7단계에 「이 수치가 사는 자리는 PR 본문 `## 검증` 절 하나이며 리뷰 답글로 옮겨 적지 않는다」를 덧붙였다. Provenance: 2026-09-08 team-lead — 「구현과 어떤 테스트를 통과했다가 아니라 pr comment는 명확해야해, 지금 테스트 통과한 것은 굳이 말할 필요가 없잖아」.
- 2026-09-08 — v1.6.1: v1.6.0의 AP-17 예시 `돌아가서 확인`이 화면 하나의 실수처럼 적혀 있었는데, 실제로는 확인 다이얼로그 셋 이상(`program-authoring-confirmation-dialog.tsx:36`, `application-confirmation-dialog.tsx:61`, `milestone-document-resubmission-dialog.tsx:69`)이 공유하는 저장소 전역 관행이었고 `milestone-document-resubmission-dialog.tsx:15-16`의 설계 주석이 그 선택을 명시적으로 방어하고 있었다 — 이대로 두면 이 문서를 읽고 다이얼로그 하나만 고친 사람이 취소 라벨을 갈라 AP-2·AP-14를 새로 만드는, 안티패턴 목록이 다른 안티패턴을 낳는 지시가 될 뻔했다 → AP-17의 그 문단을 현재 상태(공유 라벨 + 방어 근거)·PM 판단(라벨이 행동 하나보다 길면 문장을 지고 있다)·고치는 법(공유 라벨은 쓰는 곳 전부와 설계 주석을 한 PR에서 같이 바꾸거나 손대지 않는다, 판단은 AP-20처럼 티켓으로 올린다) 세 부분으로 다시 쓰고, 「하지 않는 것」에 여러 화면이 공유하는 라벨을 한 화면에서만 고치지 않는다는 줄을 더했다. SKILL.md 8번 절차의 요약 줄도 `세면 끝나는 넷 / 화면을 열고 읽는 넷`이라 스무 항목 중 A·B(8개)만 세고 C·D(12개)가 빠져 있던 것을 `세면 끝나는 넷 / 나머지 열여섯`으로 바로잡아 문서 서문의 표현과 맞췄다. AP-18의 Tooltip 실사용 주장(`program-milestone-card.tsx:58-85` 등 5곳)은 재확인 결과 맞아 그대로 두었다. Provenance: 2026-09-08 team-lead 요청 — AP-17 예시가 잘못 일반화될 위험과 SKILL.md 8번 요약 줄의 개수 불일치를 지적.
- 2026-09-08 — v1.6.0: PR #1230(제출·다운로드 화면) 리뷰에서 문구를 늘려 리뷰 지적을 덮는 패턴 — 파일 오류 안내문이 바로 아래 버튼 라벨을 그대로 되풀이하고, 펼침 토글이 「다운로드 범위」를 세 번 말하고, 라디오 선택지가 위젯이 이미 보장하는 「하나」를 다시 적고, 「무엇이 바뀌었나」를 답하지만 아무도 묻지 않는 화면이 문구만 다듬어져 나온 것 — 이 자동 점검과 기존 판정 표를 모두 통과했다 → UX 안티패턴에 「D. 글자를 늘려 고친 넷」(AP-17 컨트롤을 설명하는 문장, AP-18 아이콘이면 끝나는 문장, AP-19 위젯이 이미 말한 것, AP-20 묻지 않은 질문)을 더해 스무 항목이 됐다. PR 열기 전 절차 9단계로 `/ponytail-review`를 넣어 diff의 과잉 구현을 거르게 했다 — UX 안티패턴이 화면에서 글자를 줄이는 것이라면 ponytail은 diff에서 코드를 줄이는 같은 규칙이다. 판정 표 근거 칸에 「A를 B로 바꿨다」는 문구 치환만 적지 않도록 규칙을 못박고, 그 예시였던 AP-6 줄을 구조적 수정 서술로 고쳤다. Provenance: 2026-09-08 team-lead 요청 — PR #1230 리뷰 관찰과 ponytail 플러그인 도입을 스킬에 반영할 것.
- 2026-09-07 — v1.5.0: 여덟 항목이 한 화면 안의 **문구와 수**만 봐서, 프로그램 편집·상세에서 실제로 사람이 지적한 결함을 하나도 잡지 못했다 → 구조와 동작을 보는 여덟(AP-9~AP-16)을 「C. 편집·상세 화면에서 관찰된 여덟」로 추가했다. 죽은 컨트롤(눌러도 no-op인 `aria-pressed` 선택기), 사라진 기능의 잔상(폐기된 신청서 양식·버전 표시), 의미 없는 시각(시작·마감 라벨 없는 날짜), 뒤섞인 출처(운영자 공지와 제품 안내가 같은 회색 문단), 뒤집힌 노출(제출 항목은 접히고 달력은 상시), 갈라진 편집(한 마일스톤을 다이얼로그와 즉시 저장으로 나눠 고침), 어긋난 저장 범위(취소가 되돌리지 못하는 즉시 저장), 닿지 않는 본문(편집 본문 79px 붕괴)이다. 판정 표와 게이트를 열여섯 줄로 늘리고, 문서 끝에 NN/g·GOV.UK·W3C(WAI-ARIA/WCAG 2.2)·Norman 원문 링크를 「참고」로 모았다 — 주장 본문은 복사하지 않는다. Provenance: 2026-09-07 운영자 요청 — 「내가 별로라고 말하는 것들」을 PR UX 점검 리스트에 넣고 실제 UX 레퍼런스 링크를 마지막에 붙일 것.
- 2026-09-04 — v1.4.0: 게이트는 옳았는데 통과할 방법이 적혀 있지 않아, QA 회차 하나에서 같은 자리에 반복해서 걸렸다 → 네 가지를 인코딩했다. (1) **캡처 첨부가 사람 손을 요구한다는 서술을 정정했다** — `gh release upload`로 **이미 발행된** release에 파일을 더하는 것은 배포 트리거(`release: types: [published]`)를 쏘지 않으므로 안전하고, PM이 PR #1186에서 실제로 쓰던 방법이다. `gh release create`만 금지로 남긴다. (2) [`references/evidence-harness.md`](references/evidence-harness.md)를 새로 두어 local-review 하네스로 Before/After를 같은 조건에서 얻는 법, 요소 캡처가 빈 화면으로 나오는 세 원인, 같은 이름의 버튼이 여럿일 때 눌리는 것을 고르는 법, 빈 상태 세 갈래를 **실제로 서로 다르게** 만들어 확인하는 법, 그리고 PR 본문을 따옴표 heredoc으로 만들어야 하는 이유(따옴표 없는 heredoc이 백틱을 실행해 `deletionProtected`가 통째로 사라진 채 발행된 적이 있다)를 적었다. (3) **찍을 수 없는 상태는 fixture를 채워서 찍는다**를 게이트의 일부로 명시했다 — 아무도 눈으로 확인할 수 없는 화면은 조용히 낡고, 실제로 fixture를 채우다 「값이 없으면 조용히 기본값으로 떨어지는」 결함이 드러나 캡처 게이트가 아니었으면 통과했을 변경을 잡았다. (4) 절차 7단계에 **「초록불이 돌았다는 뜻은 아니다」**를 넣었다 — 없는 워크스페이스 이름으로 필터하면 pnpm이 아무것도 실행하지 않고 exit 0을 내고, 배열 플래그 뒤 경로가 ignore 값으로 먹혀 고친 파일이 조용히 제외되며, rebase 충돌을 풀다 옛 코드가 새 코드보다 먼저 반환하는 상태를 lint·typecheck가 둘 다 통과시키고 전체 테스트만 잡았다. 그래서 검증 보고에 실행된 suite·test 개수를 적게 했다. 더해서 push를 막는 로컬 관문 두 개(`docs/handoff/team-state/` 저널 항목, 저장소 전체 prettier)를 절로 분리하며 저널과 PR 본문의 독자가 다르다는 것을 밝혔고, 인터뷰가 **티켓의 방향 자체를 뒤집는 경우** 티켓 본문의 `할 일`도 다시 쓰고 앞선 방향의 코드를 걷어내도록 했으며, 안티패턴 자동 점검에 가시성 필터를 넣어 반응형 중복 내비게이션 거짓 양성을 없애고 구조적 반복을 위반으로 적지 않도록 판정 지침을 더했다. Provenance: PM이 PR #1190을 읽고 「이번 PR 만드시면서 더 고려하신 부분이 있다면 저희 PR skill에 반영 주실 수 있을까요」라고 요청 — QA 회차 #1081의 PR 8건을 만들며 실제로 걸린 자리를 스킬로 옮겼다.

- 2026-09-03 — v1.3.0: PR 본문이 증거를 갖춰도 사람이 읽을 수 없었다 — PR #1171은 Before 캡처 0장·구현 언어 요약·`\title` 렌더 깨짐으로 도착했는데 required CI는 전부 통과했고, 배포 화면 `/programs/<id>/status`는 `전체 미제출 5팀`을 통계와 필터에 글자 그대로 두 번 그리고 있었다 → Nielsen 10 usability heuristics에 앵커링한 [`references/ux-antipatterns.md`](references/ux-antipatterns.md) 여덟 항목(자동 점검 4 + 사람 판단 4)을 절차 8단계 게이트로 넣고, 캡처 게이트에서 `N/A`를 없애 화면을 건드린 모든 변경에 Before/After 표·전체 화면 2장·`jnu-oss-hub.com` 확인 링크를 요구하며, PR 본문 순서를 사람이 읽는 것 우선으로 고정해 `## 고친 파일`을 맨 아래로 내리고 섹션 번호를 없앴다. 본문을 렌더된 상태로 다시 읽는 원칙과 완료 체크리스트 항목도 함께 추가했다. Provenance: PM이 PR #1171과 배포 화면을 읽고 「학생이 첫 QA, 나는 마지막 product gate」로 책임을 옮겨 달라고 요청 — 안티패턴을 스킬에 인코딩해 아무도 몰라도 지켜지게 할 것.

- 2026-09-02 — v1.2.0: `manage-qa-tickets`가 티켓 본문 계약의 세 실행 계약 섹션 이름을 `할 일`·`하지 않을 것 (이 티켓의 경계)`·`완료 조건`으로 바꿔 이 스킬의 계약 파싱 절이 옛 이름만 찾고 있었다 → 절차 2단계와 이스케이프 해치에 새 이름을 원본으로 두고 옛 이름을 괄호로 병기해 2026-09-02 이전 발행 티켓도 계속 파싱하게 했다. Provenance: PM이 발행된 QA 티켓을 읽고 가독성 문제를 지적 — `manage-qa-tickets`의 섹션 이름 변경에 맞춰 정합.
- 2026-09-02 — v1.1.0: PR 본문이 증거는 갖췄어도 "무엇을 고쳤다" 식 구현 언어로 쓰여 리뷰어가 읽기 힘들었다 → `## 증거 게이트`와 `## PR을 연다` 사이에 `## PR 본문 작성 원칙` 절을 추가해 페르소나 언어 요약, 사람이 확인한 행동 중심 검증 서술, 로컬 환경 잡음 배제(단 실행 못 한 검증은 예외), 중첩 불릿·클릭 가능한 파일 링크·정합된 섹션 번호, 미첨부 placeholder 금지를 규정하고 완료 체크리스트에 두 항목을 추가했다. Provenance: PM이 PR #1058 본문을 읽고 가독성 문제를 지적 — 재발 방지를 위해 스킬 규칙으로 명문화 요청.
- 2026-09-01 — v1.0.0: PR 제출 전 실행 절차가 `manage-qa-tickets/references/execution-workflow.md`에 갇혀 있어 티켓 없이 이미 구현된 변경을 PR로 낼 때도 그 스킬 전체를 거쳐야 했고, backend 로직 변경은 frontend와 달리 어떤 증거도 요구받지 않았다 → 계약 파싱·구현·완료 조건 실증·PR 흐름을 `submit-pr-evidence`로 독립시키고, 기존 frontend Before/After 캡처 게이트 옆에 backend 로직 변경용 mermaid·DOT 흐름 다이어그램 게이트를 새로 추가했다. Provenance: 2026-09-01 운영자 요청 — PR 제출 전 스킬을 독립시키고 backend 로직 변경에 mermaid/DOT 다이어그램을 요구할 것.
